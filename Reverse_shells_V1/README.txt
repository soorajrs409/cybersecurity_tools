Reverse_shells_V1 is used for remotely connecting to other computers.

The server_4_1 is designed to accept only one connection at a time.

server_4_many is designed for accepting many connections at the same time.

server_4_many uses threading for accepting many connections at the same time.

The client and server can be converted into executables using pyinstaller.

NOTE : Designed to be executed from the terminal