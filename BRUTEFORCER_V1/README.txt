The ssh_bruteforcer_V1 is a tool that can be used to bruteforce the ssh passwords.

ssh_bruteforcer_V1 uses threading for fast and efficient bruteforce.

The user can enter the ip, username and the password file or the path for the password file.

This tool is designed to be executed from commandline.